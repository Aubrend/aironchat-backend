[build]
  command = "echo Building Functions"
  functions = "netlify/functions"
  publish = "public"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["express", "pg", "bcryptjs", "jsonwebtoken", "cors", "dotenv", "axios"]
  included_files = ["src/**"]

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true